#include <stdio.h>
/**
 * main - Write a program that prints _putchar, followed by a new line
 *
 *
 * Return: always 0 (success)
 */

int main(void)
{
	int a;

	for (a = _putcha;)
	{
		putchar(a)
	}
	putchar('\n');
	return (0);
}
